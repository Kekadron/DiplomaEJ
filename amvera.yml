version: 1.0

services:
  web:
    type: python
    python_version: "3.13"
    entrypoint: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    build_command: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate